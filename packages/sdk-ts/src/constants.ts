/**
 * Default endpoints and version constants for the Wattz SDK.
 */

export const DEFAULT_BASE_URL = 'https://api.wattz.fi/v1';

export const DEFAULT_TIMEOUT_MS = 60_000;

export const DEFAULT_MAX_RETRIES = 2;

export const SDK_VERSION = '0.1.0';

export const USER_AGENT = `wattz-sdk-js/${SDK_VERSION}`;

export const API_KEY_ENV = 'WATTZ_API_KEY';

export const BASE_URL_ENV = 'WATTZ_BASE_URL';
